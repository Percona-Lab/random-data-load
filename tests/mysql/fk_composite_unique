CREATE TABLE t1(id int PRIMARY KEY, label varchar(50));
CREATE TABLE t2(id int PRIMARY KEY, label varchar(50));
CREATE TABLE t3(
	t1_id int NOT NULL,
	t2_id int NOT NULL,
	qty int,
	PRIMARY KEY (t1_id, t2_id),
	FOREIGN KEY (t1_id) REFERENCES t1(id),
	FOREIGN KEY (t2_id) REFERENCES t2(id)
);
