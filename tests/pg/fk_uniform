CREATE TABLE t2(
	id bigint generated always as identity primary key,
	data varchar(30)
);

CREATE TABLE t1(
	id bigint generated always as identity primary key,
	t2_id bigint references t2(id),
	data varchar(30)
);
